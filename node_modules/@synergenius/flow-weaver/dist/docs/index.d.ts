export interface DocTopic {
    slug: string;
    name: string;
    description: string;
    keywords: string[];
}
export interface DocContent {
    slug: string;
    name: string;
    description: string;
    keywords: string[];
    content: string;
}
export interface DocSection {
    heading: string;
    level: number;
    content: string;
    codeBlocks: string[];
}
export interface DocStructured {
    slug: string;
    name: string;
    description: string;
    keywords: string[];
    sections: DocSection[];
}
export interface SearchResult {
    topic: string;
    slug: string;
    section: string;
    heading: string;
    excerpt: string;
    relevance: number;
}
/**
 * List all available documentation topics.
 */
export declare function listTopics(): DocTopic[];
/**
 * Read a single documentation topic.
 * @param slug - Topic slug (filename without .md)
 * @param compact - If true, return a compact LLM-friendly version
 */
export declare function readTopic(slug: string, compact?: boolean): DocContent | null;
/**
 * Read a topic and return structured sections (for JSON output).
 */
export declare function readTopicStructured(slug: string): DocStructured | null;
/**
 * Search across all documentation topics.
 * Returns matching sections with context.
 */
export declare function searchDocs(query: string): SearchResult[];
//# sourceMappingURL=index.d.ts.map